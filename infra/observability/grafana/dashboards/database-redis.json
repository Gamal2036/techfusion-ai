{
  "annotations": { "list": [] },
  "editable": true,
  "panels": [
    {
      "title": "PostgreSQL Connection Status",
      "type": "timeseries",
      "gridPos": { "h": 10, "w": 12, "x": 0, "y": 0 },
      "targets": [
        { "expr": "rate(db_connection_attempts_total[5m])", "legendFormat": "{{outcome}}" }
      ]
    },
    {
      "title": "DB Query Errors",
      "type": "stat",
      "gridPos": { "h": 10, "w": 6, "x": 12, "y": 0 },
      "targets": [
        { "expr": "rate(db_query_errors_total[5m])", "legendFormat": "{{service}}" }
      ]
    },
    {
      "title": "Redis Connection Status",
      "type": "timeseries",
      "gridPos": { "h": 10, "w": 12, "x": 18, "y": 0 },
      "targets": [
        { "expr": "rate(redis_connection_attempts_total[5m])", "legendFormat": "{{outcome}}" }
      ]
    },
    {
      "title": "Redis Command Failures",
      "type": "stat",
      "gridPos": { "h": 10, "w": 12, "x": 0, "y": 10 },
      "targets": [
        { "expr": "rate(redis_command_failures_total[5m])", "legendFormat": "{{service}}" }
      ]
    }
  ],
  "schemaVersion": 39,
  "tags": ["techfusion", "database", "redis"],
  "title": "Database and Redis",
  "uid": "tf-db-redis"
}
