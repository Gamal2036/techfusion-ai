# 00 — Current State

Status date: 2026-08-16. Branch `fix/acc-sec-02b1-mfa-core-hardening`. Latest mission: **ACC-SEC-02B1 — MFA Core Security Hardening** — IMPLEMENTED (backend, verified): MFA secrets are now **encrypted at rest** (`enc:v1:` envelope-encrypted base32 via the existing AES-256-GCM `EncryptionService`, key = `MASTER_KEY || AI_ENCRYPTION_KEY`); legacy plaintext base32 rows stay readable and are transparently **upgraded to encrypted only after a successful possession-proven verification**; decryption failure **fails closed** (deterministic 500, no leak, login challenge denied); `POST /mfa/enroll` + `POST /mfa/verify` are now **throttled** (5/60 s per IP, wired from the previously-unused `STRICT_RATE_LIMITS.mfa`, deterministic 429 proven in tests) with no impact on login or unrelated routes; enrollment lifecycle is deterministic (409 enroll/verify while enabled, 400 not-enrolled, pending re-enrollment allowed, enabled secret never silently replaced); structured security events `mfa_enrollment_started` / `mfa_enabled` / `mfa_verification_failed` emitted (no credential material ever logged). New focused suite `test/mfa-security.spec.ts` (24 tests, covering all 17 required proofs). Evidence: full api-gateway suite 62 suites / 1057 tests PASS, lint + build clean, `git diff --check` clean, V1 gate 19/19 PASS. **Committed locally; PUSHED: NO; PRODUCTION DEPLOYED: NO.** Prior mission **ACC-FIX-01 — fix Account production Slot runtime crash** — IMPLEMENTED (shared UI fix + regression tests, verified): the shared `@techfusion/ui` `Button` renders its children as a multi-expression block (loading spinner, left/right icon spans, children — including falsy results), so even a single `asChild` child (the OrganizationSection "Manage organization" `Link`) reached Radix `Slot` as an ARRAY. `React.Children.count` returned 1 but `React.isValidElement(array)` was false, so `@radix-ui/react-slot@1.3.0` threw `Slot failed to slot onto its children. Expected a single React element child or 'Slottable'.` the moment org data resolved and the dashboard error boundary replaced the Account page. Fix: when `asChild`, `Button` now slots the semantic child through Radix **`Slottable`** (exactly one Slottable target); loading/leftIcon/rightIcon remain as Slot siblings per Radix's documented multi-child composition, so normal rendering, loading/loadingText, icons, variants/sizes/fullWidth/classes/refs/handlers/a11y and Next.js Link semantics are preserved. No dependency change (`@radix-ui/react-slot` v1.3.0 already exports `Slottable`). Regression tests exercise the REAL shared Button — `packages/ui/src/__tests__/Button.test.tsx` (7 tests) + `apps/web/src/__tests__/organization-section.spec.tsx` (3 tests), both failing pre-fix, plus existing `account-page.spec.tsx` href assertions. Evidence: ui suite 35 suites / 429 tests PASS, full web suite 39 suites / 824 tests PASS, `pnpm lint` + `pnpm build` clean, `git diff --check` clean, V1 gate 19/19 PASS. **Committed locally; PUSHED: NO; PRODUCTION DEPLOYED: NO; MANUAL VERCEL CERTIFICATION: PENDING.** Prior mission **ACC-FOUND-01 — Account foundation** — IMPLEMENTED (web + minimal backend, verified): `/dashboard/settings/account` is now a capability platform, not a single-purpose deletion page. Sections: Profile Information (real server data from new self-scoped `GET /auth/account/summary` — display name, email, account ID, created/updated; display name editable via `PATCH /auth/account/summary`, trimmed 1–100 chars, forged body `userId` stripped, identity from `req.user.sub` only), Security & Sessions (MFA status from the authoritative `GET /mfa/status`; password change and session listing honestly shown as "not available in this release" — no fabricated controls), Organization context (`GET /organizations/current` via `useCurrentOrganization`, membership role read-only, link to org settings), and the CERTIFIED Danger Zone (deletion preview/confirm preserved verbatim — sole-Owner block, co-Owner eligibility, exact `DELETE` confirmation, auth cleared + redirect). New backend surface: `AccountProfileService` returning only safe `User` fields (id/email/displayName/createdAt/updatedAt) — no secrets, no schema migration. Tests: `test/account-summary.spec.ts` (10 backend tests) + `account-page.spec.tsx` (18 web tests) green; full web suite 38 suites / 821 tests PASS; `pnpm lint` + `pnpm build` clean. **Committed locally; PUSHED: NO; PRODUCTION DEPLOYED: NO.** Prior mission (committed + merged via PR #1, `52331a0`): `DEV-REV-01` device credential revocation & recovery. Baseline (unchanged): `NET-01A` IMPLEMENTED_PENDING_REAL_DEVICE_CERTIFICATION; **CYBER-01 CLOSED for V1**; V1-STAGE-01 CLOSED.

## 1. Verified Baseline

| Claim | Verdict | Evidence |
|-------|---------|----------|
| V1 CI/CD local gate is green | `VERIFIED_THIS_RUN` (local) | `scripts/ci-v1-gate.sh` 19-step gate; certification reports `docs/v1/V1-STAGE-01C_CICD_RELEASE_GATE_CERTIFICATION_REPORT.md`, `V1-STAGE-01C-R4_DOCKER_PACKAGING_GATE_REPORT.md` report 19/19 PASS and 913 API / 790 web / 79 worker / 78 agent tests |
| GitHub-native CI green | `UNVERIFIED` | Both STAGE-01C reports explicitly state `GITHUB RUN PENDING`; CI-REAL-01..07 deferred to an operator push. No GitHub run evidence in git |
| ci-api / ci-web / ci-worker / ci-agent / ci-bootstrap / ci-migration / ci-security / v1-green-gate jobs | `INFERRED_FROM_CODE` | `.github/workflows/ci.yml` defines all eight; fail-closed gate at lines 318-348 |
| Docker matrix builds green (gateway/web/worker/agent) | `INFERRED_FROM_CODE` (local gate step) | `ci.yml:351-437`; Dockerfiles exist for all four services; local gate verifies image builds |
| GHCR publishing authorized | `VERIFIED_THIS_RUN` | Commit `c4d56bf fix(ci): authorize GHCR package publishing`; login/package-write permissions in `ci.yml:362-364,412-417` |
| Linux agent enrollment/reconnection manually tested | `VERIFIED_BY_CURRENT_CI` | `docs/v1/V1-STAGE-00B-R2_BETA4_RELEASE_REAL_DEVICE_CERTIFICATION_REPORT.md`, `V1-ENROLL-01A_LINUX_ZERO_TOUCH_ENROLLMENT_REPORT.md`, `V1-STAGE-01B_PRESENCE_TELEMETRY_RELIABILITY_REPORT.md` |
| Persistent device credentials exist | `VERIFIED_THIS_RUN` | `apps/agent/src/registration.rs` (token/device_id files, 0600); `Device.deviceTokenHash` + rotation events server-side |
| Organization / membership / RBAC / monitoring work exists | `VERIFIED_THIS_RUN` | `OrganizationMember` model (migration `20260807000000`), `Permission` catalog ~40 keys, monitoring/presence sweep, alert lifecycle |

## 2. Git State

- `git status --short`: clean working tree — ACC-SEC-02B1 committed locally (`fix(security): harden MFA secret lifecycle`, 8 files: mfa service/controller/module + `mfa-secret.util.ts`, auth service/module, `rate-limits.ts`, `test/mfa-security.spec.ts`). The intended single untracked file `apps/api-gateway/.env.test` (test placeholders, never modified, never staged) remains untracked. **PUSHED: NO; PRODUCTION DEPLOYED: NO; MANUAL VERCEL CERTIFICATION: PENDING.**
- ACC-SEC-02B1 MFA core security hardening committed locally on branch `fix/acc-sec-02b1-mfa-core-hardening` — MFA secrets encrypted at rest (`enc:v1:` envelope), legacy plaintext readable + upgraded after verified possession, fail-closed decrypt, enroll/verify throttled 5/60 s (deterministic 429), deterministic lifecycle (409/409/400), structured security events; `test/mfa-security.spec.ts` 24 tests; full api-gateway suite 62/1057 green, lint/build clean, V1 gate 19/19 PASS. Pending: operator push.
- ACC-FIX-01 Account Slot runtime crash fixed and committed locally on branch `feat/acc-found-01-account-foundation` — shared `Button` now slots `asChild` children through Radix `Slottable`; regression tests use the real Button (7 ui + 3 web tests, failing pre-fix); ui 35/429, web 39/824, lint/build clean, V1 gate 19/19 PASS. Pending: operator push + manual Vercel certification.
- ACC-FOUND-01 account foundation committed locally on branch `feat/acc-found-01-account-foundation` — web sections + minimal self-scoped backend summary + focused specs (10 backend + 18 web tests) + full web suite green (38 suites / 821 tests), lint/build clean. Pending: operator push.
- Latest commit: `fix(security): harden MFA secret lifecycle` (ACC-SEC-02B1, committed locally; PUSHED: NO; PRODUCTION DEPLOYED: NO).
- Previous: `fix(ui): make Button asChild slot-safe` (ACC-FIX-01, committed locally; PUSHED: NO; PRODUCTION DEPLOYED: NO).
- Previous: `feat(account): build verified account foundation` (ACC-FOUND-01, committed locally; PUSHED: NO; PRODUCTION DEPLOYED: NO).
- Previous: `52331a0` — Merge pull request #1 (DEV-REV-01 device revocation & recovery, merged).
- Previous: `5cb63d1 docs(device): document support recovery configuration`, `5858270 fix(device): add credential revocation and recovery foundation` (DEV-REV-01 commits).
- Previous: `release(agent): prepare v1.0.0-agent-beta.5` (`e0cb494`).
- Previous: `docs(network): establish verified V1 network baseline` (NET-00) — audit-only docs commit: the NET-00 report + minimal canonical corrections (`00`, `08`, `12`, `PRD.md` §6.4). No runtime code changed.
- Previous: `docs(cybersecurity): certify real-device V1 flow` (CYBER-01 closure) — manual real-device certification PASS recorded, CYBER-01 CLOSED for V1, Cybersecurity preserved as the V1 stable baseline, docs-only closure.
- Previous: `fix(cybersecurity): close real-device scan polling failures` (`340a15e`, V1-STAGE-02-SUB-01A / CYBER-01) — Cybersecurity Web terminal-state machine (idle/triggering/running/completed/failed/timeout) in `useSecurity.ts`, `GET /security/latest` now exposes failed terminal scans backward-compatibly, agent Bearer-header regression test, focused web/API regression tests, report + living-doc updates.
- Previous: `fix(integration): align device-backed product data flows` (V1-STAGE-02-SUB-01A) — security push-path fail-closed 401, network page dead-endpoint removal + unknown-MAC truthfulness, focused test updates, integration certification report + living-doc updates.
- Earlier: `fix(device): certify enrollment and device-link reliability` (V1-STAGE-02-SUB-01) — schema/migration, devices/enrollment service hardening, E1-E8 suite, presence truthfulness, docs.
- Recent history: GOV-01 governance foundation, then V1-STAGE-01 security closure (S1-S5, `V1-STAGE-01-SUB-01..05`), CI/release-gate hardening, billing/RBAC/monitoring/account work, command-center UI hardening.

## 3. What This Repository Actually Is

A working monorepo for a Linux-first device management + monitoring SaaS:

- **apps/web** — Next.js 14 command center (real API wiring, WebSocket live alerts, polling).
- **apps/api-gateway** — NestJS API (auth/RBAC/orgs/devices/enrollment/monitoring/alerts/network/inventory/security/KB/reporting/backups/remote-support/billing/AI/audit/admin).
- **apps/worker** — BullMQ worker, 8 processors (monitoring sweep, alerts, backups, inventory, security, retention, KB embedding, report).
- **apps/agent** — Rust (1.96.0) systemd Linux agent, `1.0.0-beta.5`, 88 tests, 17 verified endpoint call-sites.
- **packages/** — shared config, types, ui, utils.

## 4. Headline Findings (detail in referenced docs)

1. **Security CRITICAL — CLOSED**: SSO login accepted client-supplied identity with no IdP assertion validation — `POST /auth/sso/login` could authenticate as any email in an SSO-enabled org. Remediated `V1-STAGE-01-SUB-01` (fail-closed `501`, no tokens/JIT/link, 10 new regression tests, full API suite 923 green). SSO is **DISABLED_SAFE**, not certified. (`07`, `V1-STAGE-01-SUB-01_SSO_REMEDIATION_REPORT.md`)
2. **RLS DECIDED (Option B — app-layer authoritative, `V1-STAGE-01-SUB-02`)**: Empirically proven inert — the app DB role is `SUPERUSER` + `BYPASSRLS` (32 tables RLS-enabled, 0 FORCE, policies never consulted), no `set_config`/`OrgContextInterceptor` exists, and Prisma pooling cannot carry session settings safely. RLS stays as non-authoritative defense-in-depth (no migration, no FORCE added). Isolation is enforced by membership/device-authoritative app-layer `orgId` scoping + the `test/cross-tenant-isolation.spec.ts` regression suite (20 tests) + worker org re-verification. (`07`, `V1-STAGE-01-SUB-02_RLS_TENANT_ISOLATION_REPORT.md`)
3. **REPORT queue has no producer and its worker path hits a non-existent route**; **KB embedding silently falls back to deterministic mock vectors**. (`06`)
4. **CD (staging/production) is not deployable as written**: Helm chart `required` values not supplied, image repo/tag mismatch, `prisma db push --accept-data-loss` in prod init. (`02`, `10`)
5. **Presence/15-minute issue**: OFFLINE classification and offline alerts are, by design, only raised 15 minutes after the last heartbeat; presence sweeps run every minute. Fast UI classification (60s/5min bands) exists. (See `00` §6 and `06`)
6. **Windows agent: not implemented** — 15-gap analysis in `05_AGENT_PLATFORM_MATRIX.md`.
7. **Entitlement enforcement is partial**: devices/reports/AI-quota/feature-gates enforced server-side; `maxTeamMembers` and `maxAlertRules` defined but never enforced. (`09`)
8. **Presence truthfulness CERTIFIED (`V1-STAGE-02-SUB-01`)**: `Device.lastSeenAt` nullable; registration never implies ONLINE (UNKNOWN until a verified heartbeat); enrollment/device-link lifecycle certified by `test/enrollment-device-link.spec.ts` (E1-E8, 16 tests) — single-use/expiry/revocation tokens, reconnect→same Device, registration race collapse, strong-identity credential recovery, no hostname-only relink.
9. **Real-device integration boundaries certified, 5 defects/root causes fixed (`V1-STAGE-02-SUB-01A`)**: telemetry→Dashboard, Cybersecurity, and Network paths are REAL_AGENT_DATA end-to-end (no demo/mock/fabricated values; zero hardcoded device data in web production pages; org/device always server-derived). Fixed: **SEC-1** `POST /devices/security-report` was fail-open (HTTP 200 + error body for an invalid/revoked credential, which the agent treated as success) → now 401 fail-closed, consistent with `DeviceTokenGuard`; **NET-1** Network page polled a non-existent `/api/network/scans` endpoint (dead scan-completion logic) → now uses real `GET /network/scans`; **NET-2** unknown-MAC sentinel `00:00:00:00:00:00` was rendered as a real MAC → now `-`. `GET /security/scans/detail/:scanId` verified NOT shadowed by `:deviceId` (single-segment matching). Remaining product gaps (security push-path body-token transport, Network org-pool merge/unassigned-scan semantics, server-host diagnostics) documented, not fixed. **Manual real-device certification PASS (2026-08-11)** — operator-verified on a real Ubuntu host with the rebuilt/reinstalled agent (Dashboard recognizes the enrolled device, Cybersecurity page stable, real-device scan flow completes); CYBER-01 CLOSED for V1; Network real-device evidence deferred to `NET-00`.
10. **Cybersecurity real-device reliability fixed (CYBER-01)**: the real-device manual gate exposed a 401 on `GET /security/pending/:deviceId` (deployed agent binary predated the Bearer header — source `apps/agent/src/client.rs:484` already correct) and a Web polling loop with no terminal state. Fixed: **CYB-1** `useSecurity.ts` now has a complete terminal-state machine (`idle`/`triggering`/`running`/`completed`/`failed`/`timeout`) — completed/failed stop polling, a 120 s backstop turns a stuck pending scan into an honest timeout/retry state, and 401/403 + unexpected API failures stop polling with an honest error banner (never infinite loading); **CYB-2** `GET /security/latest/:deviceId` now returns the latest terminal scan (`completed|failed`) and exposes the scan `error`, backward-compatibly. Agent change was test-only (source already correct); the previously-installed binary on the real host was stale — **REBUILT/REINSTALLED and manually certified PASS (CYBER-01 CLOSED for V1)**; the permanent-loading/timeout behavior is resolved in real-device testing. Cybersecurity remains the V1 stable baseline; future Cybersecurity capabilities stay extensible under the Module Extensibility Contract (`15`).
11. **Network baseline established (`NET-00`, 2026-08-12)**: full Network path traced in current code, audit-only. Security boundary certified intact (`DeviceTokenGuard` on all agent routes, JWT+RBAC on web routes, scan ownership enforced, cross-tenant fail-closed). Discovery data is REAL_AGENT_DATA end-to-end (ARP + private /24–/25 ICMP sweep, OUI vendor table, reverse-DNS hostnames; unknown MACs masked). **P0 proven defect NET-A01**: agent network discovery ships **default-disabled** (`config.rs:80` `TF_NETWORK_DISCOVERY=false`) and the official installer (`install-linux.sh`) writes `agent.env` without it — a stock installed real agent never polls `/network/discovery/pending`, so Web-triggered discovery silently produces nothing after the 65 s backstop (likely root cause of the known real-device Network failure). P2 gaps: NET-A02 stale-pending scans cleaned only when some agent polls; NET-A03 duplicate command+result scan rows; NET-A04 no Web terminal-state/error banner for discovery; NET-A07 unvalidated agent ingress DTO. Diagnostics confirmed to run from the API-gateway host (unpersisted, vantage undisclosed). Network is **NOT COMPLETE**; next = NET-01A (P0), NET-01B, NET-01C. (`NET-00_NETWORK_EVIDENCE_CURRENT_STATE_REPORT.md`)
12. **NET-01A P0 fix implemented (`2026-08-12`)**: (a) agent network discovery **ENABLED by default** (`config.rs:80` `TF_NETWORK_DISCOVERY=true`), installer writes `TF_NETWORK_DISCOVERY=true` while preserving an explicit operator opt-out, enabled+identified agents enter `poll_pending_discovery_commands` via a `should_poll_network_discovery` gate — device Bearer auth unchanged; (b) **Web-vs-Agent auth/polling boundary closed**: real-device logs showed the Web stuck on "Discovering..." polling `GET /network/discovery/pending` → `DeviceTokenGuard` 401 (no device token in the browser). Web discovery now triggers via user JWT `POST /network/discovery/trigger` and polls the user-JWT read path `GET /network/scans` only; the device token is never exposed to the browser; `DeviceTokenGuard` is preserved for agent-only routes. `useStartDiscovery` is a full terminal-state machine (`idle/triggering/running/completed/failed/timeout`) — completed/failed stop polling, a 90 s backstop turns a stuck scan into an honest timeout, 401/403 + unexpected failures stop polling with an honest error banner, and a stale in-flight status write cannot overwrite a terminal state (never infinite loading). Focused Web-vs-Agent regression tests: API F2 (user JWT rejected 401 on `/network/discovery/pending`; user JWT read path org-scoped 200) + web `use-network-discovery.spec.ts` (5 tests, asserts `/network/discovery/pending` is never requested). **V1 gate 19/19 PASS** (API 999 / web 802 / worker 80 / agent 84). Network still requires real-device manual certification before COMPLETE. NET-A04 (Web terminal-state banner) is resolved as part of NET-01A.
13. **DEV-REV-01 implemented (`2026-08-15`) — stale-device credential revocation & recovery (backend foundation)**: a stale Linux device bound to an inaccessible old test account can now be safely revoked and unlinked server-side. **Support boundary**: the repo has NO `SYSTEM_ADMIN`/`SUPPORT_ADMIN` role, so the smallest secure internal boundary is `SupportAdminGuard` — `SUPPORT_ADMIN_API_KEY_HASHES` (env JSON array of SHA-256 hex) vs. the `x-support-admin-key` header, fail-closed 401, pseudo-actor `support:admin`, raw key never logged; org-JWT Owners cannot reach the recovery routes. **Routes**: `POST /admin/devices/:deviceId/revoke-and-unlink` (deviceId preferred) and `POST /admin/devices/revoke-and-unlink` (by `identityFingerprint`/`installationId`; ambiguous multi-org identity → 400 `AMBIGUOUS_DEVICE_IDENTITY`; unknown → 404 `DEVICE_NOT_FOUND`), throttled (5/min). **Transaction**: idempotent (`alreadyRevoked: true` on repeat) — sets `revokedAt`/`revokedReason`/`inactive=true`/`lastSeenAt=null`; cancels pending/running NetworkScan+SecurityScan → failed; terminates pending/active RemoteSessions → ended; writes structured `device_revoked` AuditLog with deviceId/previousOrganizationId/action/reason/requestId/correlationId — **no credential material in logs/audit/response**. **Fail-closed auth**: `DeviceTokenGuard` returns stable 401 `DEVICE_CREDENTIAL_REVOKED` for revoked credentials (before the inactive branch, verifier retained so the old credential stays identifiable but can never authenticate); `findByToken` requires `revokedAt: null`, so body-token transports (`security-report`, remote-support) fail closed too. **Duplicate-safe re-enrollment**: same-org re-enroll reuses the existing row (credential rotated, `revokedAt` cleared, `device_reenrolled` audit — the OLD credential is never reactivated); cross-org re-enroll always creates a NEW Device row in the new org, preserving the old org's revoked row as audit/history (D14 app-layer isolation). No per-device enrollment token exists (`EnrollmentToken` is org-scoped only), so no recovery-token invalidation was possible/needed. **Agent unchanged** (identity-status/reset-identity already never print the token). New focused spec `test/device-revocation-recovery.spec.ts` (18 tests); API gate green: 60 suites / 1023 tests PASS, lint/build clean, worker typecheck clean. **No production DB operations were performed. DEV-REV-01 was committed and merged into main via PR #1 (merge commit 52331a0).**

## 5. Test Evidence

| App | Files | Count (from cert reports) | Command |
|-----|-------|---------------------------|---------|
| api-gateway | 62 suites incl. `test/enrollment-device-link.spec.ts`, `test/presence-telemetry.spec.ts`, `test/sso-login.spec.ts`, `test/cross-tenant-isolation.spec.ts`, `test/device-credential-hardening.spec.ts`, `test/device-metrics-security.spec.ts`, `test/metrics-auth-security.spec.ts`, `test/tenant-isolation-security.spec.ts`, `test/device-revocation-recovery.spec.ts` (18 DEV-REV-01 tests), `test/mfa-security.spec.ts` (24 ACC-SEC-02B1 tests), `src/security/security.integration.spec.ts` | 1057 | `pnpm test` (`jest --forceExit --runInBand`) |
| web | 37 specs incl. `src/__tests__/use-security.spec.ts`, `src/__tests__/use-network-discovery.spec.ts` (5 NET-01A boundary tests) | 802 | `pnpm test` (`jest --forceExit`) |
| worker | 8 specs | 80 | `pnpm test` |
| agent | 84 tests in-source (incl. pending-security Bearer-header + NET-01A discovery-enablement/gate/Bearer-header tests) | 84 | `cargo test` |

`VERIFIED_THIS_RUN` for api-gateway (62 suites / 1057 tests green — incl. E1-E8 enrollment/device-link suite, all five Stage-01 security suites, CYB-2 `getLatestScan` regression, NET-01A F2 Web-vs-Agent boundary tests, DEV-REV-01 `test/device-revocation-recovery.spec.ts` 18 tests, and ACC-SEC-02B1 `test/mfa-security.spec.ts` 24 tests) on `DEV-REV-01`; re-verified green on `ACC-SEC-02B1`; `pnpm lint` + `pnpm build` green (api), worker typecheck green after schema sync. Prior certified baselines (unchanged): web (37 suites / 802 tests), worker (8 suites / 80 tests), agent (84 in-source tests), `scripts/ci-v1-gate.sh` 19/19 PASS on NET-01A (incl. migration validation + worker schema sync + secret scan — NO SECRETS DETECTED). **Manual real-device Cybersecurity certification: PASS (operator, 2026-08-11)** — `VERIFIED_THIS_RUN` on a real Ubuntu host with the rebuilt agent binary (report §9.1). **Network real-device manual certification: PENDING (required for NET-01A closure).**

## 6. Device Presence Finding (summary)

- Agent heartbeat = metrics POST every ~30 s (`apps/agent/src/config.rs`); server writes `Device.lastSeenAt` on every authenticated ingest (`apps/api-gateway/src/devices/devices.service.ts:325-328`).
- **Presence truthfulness (V1-STAGE-02-SUB-01): `Device.lastSeenAt` is now nullable with no default.** Registration alone no longer writes a lastSeenAt — a Device row never implies ONLINE. A registered-but-never-heartbeat device derives **UNKNOWN** (verified by `test/enrollment-device-link.spec.ts` E1/E6 and `test/presence-telemetry.spec.ts` P1-P4). Migration `20260810120000_device_lastseen_nullable_presence_truth` (DROP NOT NULL + DROP DEFAULT); existing timestamps preserved (they reflect real prior heartbeats). Worker schema copy synced (`scripts/sync-prisma-schema.sh`).
- Classification bands (client & worker, mirrored): ONLINE ≤ 5 min, DEGRADED 5-15 min, OFFLINE > 15 min, UNKNOWN = no verified heartbeat (`apps/web/src/lib/device-presence-state.ts`, `apps/worker/src/presence-state.ts`).
- Presence sweep: cron every minute + Redis lock → `MONITORING` queue → worker evaluates rules and creates/reopens OFFLINE alerts only when a device crosses the 15-minute boundary; UNKNOWN devices are never flagged OFFLINE.
- **Root cause of the observed "~15 minutes"**: the OFFLINE threshold is 15 minutes by design; an offline device is therefore not flagged (UI stays ONLINE→DEGRADED) until 15 minutes after the last heartbeat, and the offline *alert* is not emitted until the sweep observes the crossing. Web UI poll is 15 s so UI latency itself is not the cause. Secondary contributors: any failure that stops metrics ingestion (401s after 3 consecutive auth failures stop telemetry; gateway/queue downtime) inherits the same 5/15-min detection window.
- Recommendation path (do not destabilize enrollment): add an optional DEGRADED/warning presence rule tier, make the sweep threshold/step tunable per rule, and document OFFLINE latency as inherent to the 15-min band. See `06_WORKER_QUEUE_MAP.md` §Presence.

## 7. Working-Tree Hygiene

`apps/api-gateway/.env.test` remains untracked (intended, D9). DEV-REV-01 stages explicit files only (schema + migration, admin support guard/recovery controller/service/DTO/module, device-token guard + devices service changes, structured-logger additive field, worker schema sync, focused spec, docs). No secrets staged; no `.env*` staged; `SUPPORT_ADMIN_API_KEY_HASHES` added only to `.env.example` with an empty placeholder.
