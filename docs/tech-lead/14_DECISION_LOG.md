# 14 — Decision Log

Chronological record of architectural and governance decisions. First entry covers the V1-MASTER-ROADMAP-00 discovery mission (2026-08-09).

| # | Date | Decision | Rationale | Status |
|---|------|----------|-----------|--------|
| D1 | 2026-08-09 | Documentation-only discovery mission; no product code changed, no refactors, no deletions. | Mission mandate; evidence must precede change. | ✅ Done |
| D2 | 2026-08-09 | `docs/tech-lead/` is the authoritative technical-lead documentation area; legacy root `TF_*.md` reports and `TECHFUSION_V1_READINESS_AUDIT.md` are historical, not authoritative. | Single source of truth; avoid duplicate conflicting docs. | ✅ Done |
| D3 | 2026-08-09 | Highest commercial tier is referenced as `PREMIUM_TIER_PLACEHOLDER`; the internal name "GM1" is not used as a commercial name. | Founder will rename; no hard-coded prices. | ⏳ Open (founder names tier) |
| D4 | 2026-08-09 | V1-STAGE-01 (security/tenancy/credentials) is the next stage; SSO remediation is the first substage. | S1 is an active auth bypass and gates everything. | ⏳ Pending implementation |
| D5 | 2026-08-09 | RLS decision deferred to Stage 01: either implement (transactional `set_config` + non-owner role + FORCE) or remove decorative policies + app-layer isolation suite. | RLS is inert today; both options are valid with different risk. | ⏳ Open (founder/AI lead decision at Stage 01) |
| D6 | 2026-08-09 | Windows agent is P0 because "Linux AND Windows production support" is an explicit launch requirement. | Mission constraint. Founder may re-scope to P1. | ⏳ Open (founder) |
| D7 | 2026-08-09 | Stage ordering differs from the brief: identity/RBAC moved into existing certified base (not a new early stage); deploy/CD reliability and billing moved earlier; Windows placed after fleet-reliability groundwork. | Evidence shows RBAC/membership already certified; CD is undeployable; billing gaps are paid-launch gates. | ✅ Accepted |
| D8 | 2026-08-09 | Free-tier feature surface (whether network/inventory/security are free or PRO) requires founder sign-off before Stage 03 entitlement work. | Current `PLAN_CONFIGS` does not gate these; the mapping is a commercial decision. | ⏳ Open (founder) |
| D9 | 2026-08-09 | No commit of `apps/api-gateway/.env.test`; it remains untracked (test placeholders). | Working-tree hygiene; gitignore exception is intentional. | ✅ Accepted |
| D10 | 2026-08-09 | One documentation-only commit created for this mission. | Isolated clean docs change; per git safety rules. | ✅ Done |
| D11 | 2026-08-09 | **SSO strategy (founder): do NOT implement real SAML/OIDC for Production V1. Disable the unsafe `POST /auth/sso/login` path FAIL-CLOSED.** The incomplete login must never issue a session from client-supplied identity. SSO domain/database architecture (SsoConfig, admin config routes, User.sso*) is preserved for a future real implementation. | S1 is an active P0 auth bypass; real IdP verification is out of scope for this substage. | ✅ Done (`V1-STAGE-01-SUB-01`) |
| D12 | 2026-08-09 | Fail-closed mechanism = keep the `POST /auth/sso/login` route registered and `@Public()`, but have `SsoService.ssoLogin` throw a deterministic `501 Not Implemented` BEFORE touching any data (no org/config/user reads → zero information leak; slug/enabled/existence indistinguishable). Route removal rejected: preserving the route keeps an intact contract for the future SAML/OIDC implementation, and 501 is a better client contract than 404. Insecure code (`generateRefreshToken`, `ensureMembership`, imports) deleted so the exploit cannot be re-triggered by any caller. | Deterministic, safe, architecture-preserving; consistent with NestJS error architecture. | ✅ Done |
| D13 | 2026-08-09 | SSO is classified **DISABLED_SAFE** (not CERTIFIED). Re-enablement is gated on a future substage implementing the verification contract in `V1-STAGE-01-SUB-01_SSO_REMEDIATION_REPORT.md` §7 (OIDC issuer/audience/JWKS/exp/nonce+PKCE; SAML signature/issuer/audience/destination/validity/replay; JIT + linking only after verified identity). | Avoids a false "certified" claim; SSO must not be marketed/used until verified. | ✅ Done |
| D14 | 2026-08-10 | **RLS (Option B): app-layer isolation is AUTHORITATIVE; RLS is kept as non-authoritative defense-in-depth (no migration, no FORCE, no `set_config`).** Empirical proof from `V1-STAGE-01-SUB-02`: the app DB role is `SUPERUSER`+`BYPASSRLS` (32 tables RLS-enabled, 0 FORCE) so policies are never consulted; `SET app.current_org_id` + `FORCE RLS` still returned both orgs' rows to the superuser; only a `NOBYPASSRLS` non-owner role was filtered. Prisma's connection pooling cannot carry session settings safely (set_config is per-connection/transaction-only). Boundaries enforced by membership/device-authoritative `orgId` scoping + `test/cross-tenant-isolation.spec.ts` (20 tests) + worker org re-verification. | Closing D5 (founder/AI lead asked for the decision; empirical RLS proof made Option B the low-risk choice; Option A (set_config + non-owner role + FORCE) remains viable if the Prisma pooling/role model ever changes, re-run isolation suite against a non-owner role first). | ✅ Done |
| D15 | 2026-08-10 | Fix confirmed cross-tenant defects with **atomic scoped writes** (`updateMany`/`deleteMany` on `{id, orgId}`) instead of find-then-mutate, and make the **AI router state per-org** (strategy/stats keyed by `orgId`). Worker processors now **re-verify payload resource IDs against `orgId`** before acting (backup verify/restore/execute, KB embedding; security `deviceId` bound to the verified row). | Removes TOCTOU races and cross-tenant state leakage; follows the existing `processSecurityJob` safe pattern; no endpoint route/semantics changed for already-correct paths. | ✅ Done |
| D16 | 2026-08-10 | **Device credentials are verifier-only: drop the plaintext `Device.deviceToken` column and authenticate by SHA-256 `deviceTokenHash` exclusively, fail-closed.** Guard and `findByToken` have no plaintext equality fallback; register/register-public/rotate persist only the hash; the one-time raw token is returned to the device over TLS at issuance and never stored. Rows without a stored verifier fail closed (401) and recover via `POST /devices/recover-credential` / duplicate re-registration / agent re-enrollment. No plaintext→hash backfill was performed — the 20260720130000 migration already hashed all rows, and a one-way hash cannot be derived from an unknown token; `CredentialRotationEvent.oldTokenHash` uses sentinel `legacy-no-verifier` when rotating a verifier-less row. | Closes S3 (DB leak exposes live device credentials): with no plaintext column, a DB leak cannot expose a usable credential; equality fallback removed so no device can authenticate without a stored verifier; rotation/recovery keeps enrollment and re-enrollment working (kept `deviceTokenHash` nullable-@unique, no API contract change for agents). | ✅ Done (`V1-STAGE-01-SUB-03`) |
| D17 | 2026-08-10 | **Device telemetry ingestion is server-authoritative: `POST /devices/metrics` binds identity/org exclusively to the verified `Device` row (`req.device.id`/`req.device.orgId`) and treats the client payload as data only.** Malformed telemetry must fail deterministically 4xx, never 500: fractional byte/counter values and unvalidated `fans.rpm` (which previously hit `BigInt` RangeError / Prisma validation → 500) are now `@IsInt()`/nested-DTO validated; the inert body `deviceToken` field is removed (guard authenticates by header); DEV-only auth logs print token/hash prefixes no longer (presence/length/counts only). No migration required — validation-only + guard-log change. | Closes the remaining Stage-01 trust-boundary gap: with the DTO carrying no `deviceId`/`orgId` and whitelist stripping unknown fields, no client-supplied value can impersonate a device/tenant; deterministic 4xx prevents 5xx error storms from a buggy/malicious agent; raw credentials stay out of logs/errors/responses. SUB-02 isolation + SUB-03 hash-only credential guarantees preserved. | ✅ Done (`V1-STAGE-01-SUB-04`) |
| D18 | 2026-08-10 | **Metrics auth is header-only and fail-closed (S5): `GET /metrics` authenticates via `Authorization: Bearer` only; the query-string `?token=` input is removed; when `METRICS_AUTH_TOKEN` is set a non-matching/absent Bearer header → 403; in `NODE_ENV=production` an unset `METRICS_AUTH_TOKEN` → 403 (endpoint not publicly exposed until explicitly configured).** Dev (non-production, token unset) stays open for local/`prometheus.yml` scraping. Worker metrics (port 9464, cluster-internal, no ingress) keeps the query-token pattern for now — deferred, documented residual. Env is read per-request (no stale module-load snapshot). | A token sent in the query string is captured by proxies and by request-URL logging; the Helm ingress maps `api.techfusion.ai/` → api-gateway Prefix `/`, so `/metrics` is publicly reachable in the current topology. Header-only auth keeps the secret out of URLs/logs; fail-closed production requires explicit `METRICS_AUTH_TOKEN` provisioning (Stage-02 wiring adds it to Helm / ServiceMonitor). | ✅ Done (`V1-STAGE-01-SUB-05`) |
| D19 | 2026-08-10 | **Dev-only `scripts/seed-inventory-test.ts` JWT fallback is a non-production placeholder** (`tfenr_ci-secret-replace-before-deploy`, exempt from the secret scanner), not a real credential. | The previous 64-hex fallback looked like a real signing key; the placeholder is unambiguous and scanner-exempt; the script is dev-only and never deployed. | ✅ Done |
| D20 | 2026-08-10 | **Committed `backups/` artifacts and the tracked-only secret scanner are kept as documented residuals (P3/LOW).** The 60 backup artifacts were audited — no real credentials (Device tables empty, test bcrypt hashes only) — so history rewrite is not justified and retention is a founder decision (T12). The scanner stays `git ls-files`-only (T20): CI has no untracked files and `.env.test` is intentionally untracked (D9); extending it adds local false-positive risk with no CI value. Deploy-secret wiring (T1–T3) is deferred to `V1-STAGE-02`. | Fail-closed guarantees are already enforced at startup (`validateEnvironment`), in Helm (`required`), and in CI; the remaining items are deploy-wiring debt that belongs to Stage-02, not a secrets exposure. **V1-STAGE-01 is CLOSED.** | ✅ Done (`V1-STAGE-01-SUB-05`) |
| D21 | 2026-08-11 | **Stage-02 priority decision (founder): Enrollment / Token / Device-Link / Presence reliability is promoted AHEAD of Deployment/CD within V1-STAGE-02.** Trustworthy device identity is a prerequisite for Cybersecurity, Network, Monitoring and other device-backed modules. The Deployment/CD work is preserved as a LATER Stage-02 substage (`V1-STAGE-02-SUB-02`), not deleted. | Device identity/presence reliability is a capability foundation; shipping device-backed modules on an untruthful presence model would compound defects across Cybersecurity/Network/Monitoring. CD repairs remain a Stage-02 requirement, just serialized after the identity foundation. | ✅ Done (`V1-STAGE-02-SUB-01`) |
| D22 | 2026-08-11 | **Presence truthfulness: `Device.lastSeenAt` is nullable with no default and is set ONLY by a verified authenticated telemetry heartbeat.** Registration no longer writes `lastSeenAt` (previously `@default(now())`), so a Device row never implies ONLINE. A registered-but-never-heartbeat device derives **UNKNOWN**; null-safe ordering is nulls-last everywhere; worker presence-state and web presence surfaces already map null → UNKNOWN. Migration `20260810120000_device_lastseen_nullable_presence_truth` (DROP NOT NULL + DROP DEFAULT); existing timestamps preserved. Hostname is removed from device identity matching (`findExistingDevice`); credential recovery requires strong identity (identityFingerprint/installationId), hostname/deviceId alone rejected. Duplicate/concurrent registration collapses to one row (P2002 → idempotent reuse) without weakening Stage-01 hash-only credential model. | `lastSeenAt @default(now())` fabricated presence for every enrolled device — a registration with no agent heartbeat looked ONLINE. UNKNOWN is preferable to false certainty (AGENTS.md principle 3); hostname/deviceId alone cannot authorize a credential handover (principle 8); race-safe registration prevents duplicate device identities. | ✅ Done (`V1-STAGE-02-SUB-01`) |
| D23 | 2026-08-11 | **Cybersecurity real-device flow is deterministic and truthful (CYBER-01). (a) Web terminal-state machine in `useSecurity.ts`: `idle`/`triggering`/`running`/`completed`/`failed`/`timeout`.** `completed` and `failed` stop polling only when a terminal scan is NEW since the trigger (baseline-scan-id guard so a pre-existing completed scan does not prematurely end a re-run); a 120 s client backstop converts a stuck pending scan into an honest `timeout`/retry state; 401/403 and unexpected API failures stop polling and surface an honest error banner (never infinite loading); polling resets on device change; empty successful scans render truthfully ("No findings — clean posture"). **(b) `GET /security/latest/:deviceId` contract extended additively: it now returns the latest TERMINAL scan (`status IN (completed, failed)`, ordered by `completedAt` desc) and exposes the scan `error` field; pending scans still return 404 while running, and the completed path is unchanged (backward compatible).** (c) Agent: source already sent `Authorization: Bearer <persistent-device-credential>` on `GET /security/pending/:deviceId` (`client.rs:484`); change was TEST-ONLY — the deployed binary is stale and must be rebuilt for the manual retest. | The Web polling loop had no terminal model — failed/stuck pending scans left the UI permanently loading, and 401/403 never stopped polling; `getLatestScan` filtered completed-only so a failed terminal scan was invisible to polling. UNKNOWN is preferable to false certainty, and the security boundary (DeviceTokenGuard, device/org authority) is preserved — no token/auth architecture change, no fabricated findings. | ✅ Done (`V1-STAGE-02-SUB-01A` / CYBER-01) |
| D24 | 2026-08-11 | **CYBER-01 manual real-device certification PASS — Cybersecurity real-device reliability CLOSED for V1.** Operator ran the §9 manual gate on a real Ubuntu host with the agent REBUILT from current repository source and REINSTALLED (`/usr/local/bin/techfusion-agent`): persistent device identity/credential preserved (same `device_id`/`device_token`, no re-enrollment), agent service operates normally, Dashboard recognizes the real enrolled device, Cybersecurity page stable, real-device scan flow completes to a truthful terminal state, no permanent-loading/timeout. Previous behavior traced to the stale deployed binary (predated the Bearer header on the pending-security request), resolved by the rebuild. **Cybersecurity is preserved as the V1 stable baseline; future Cybersecurity capabilities remain extensible under the Module Extensibility Contract (`15`).** Network real-device evidence deferred to `NET-00`. | Real-device E2E cannot be certified by automated tests alone; rebuilt binary + PASS evidence closes CYBER-01. No runtime code, architecture, security-boundary, or product changes accompany this closure — documentation/status only. | ✅ Done (closure, 2026-08-11) |
| D25 | 2026-08-15 | **DEV-REV-01 device credential revocation & recovery foundation (support-admin boundary).** The repo has NO system-level role (`SYSTEM_ADMIN`/`SUPPORT_ADMIN` absent from `Role`/`Permission`), so the smallest secure internal boundary is a hash-only support-admin key: `SUPPORT_ADMIN_API_KEY_HASHES` env (JSON array of SHA-256 hex) compared against the `x-support-admin-key` request header, fail-closed 401 (`SupportAdminGuard`, pseudo-actor `support:admin`, raw key never logged). Routes `POST /admin/devices/:deviceId/revoke-and-unlink` and `POST /admin/devices/revoke-and-unlink` (by `identityFingerprint`/`installationId`), throttled, org-JWT Owners can never reach them. Revocation is transactional and idempotent on `Device.revokedAt`/`revokedReason` + `inactive=true` + `lastSeenAt=null`, cancels pending/running NetworkScan+SecurityScan (→ failed) and terminates pending/active RemoteSessions (→ ended), and writes a structured `device_revoked` AuditLog (requestId/correlationId; **no credential material ever logged/stored**). `DeviceTokenGuard` returns a stable 401 `DEVICE_CREDENTIAL_REVOKED` for revoked credentials (before the inactive branch), and `findByToken` requires `revokedAt: null` so body-token transports fail closed too. Same-org re-enrollment reuses the existing row (old credential rotated, never reactivated, `device_reenrolled` audit); a different-org re-enrollment always creates a NEW Device row in the new org while the old org's revoked row remains as the audit/history record. No per-device enrollment token exists (`EnrollmentToken` is org-scoped), so no recovery-token invalidation was possible/needed. | A stale device bound to an inaccessible old account must be revocable server-side without exposing a cross-org capability to org Owners (AGENTS.md principle 8) and without weakening the hash-only credential model (D16); UNKNOWN/401 is preferable to a credential that keeps working; duplicate-safe re-enrollment keeps enrollment reliable (D22) while cross-org isolation is preserved (D14 app-layer authority). | ✅ Done (`DEV-REV-01`) |
| D26 | 2026-08-15 | **ACC-FOUND-01 Account foundation: `/dashboard/settings/account` is a capability platform (not a single-purpose deletion page) and separates the personal Account from the Organization.** Page sections: Profile Information (display name + email + account ID + created/updated, editable display name via self-scoped `GET|PATCH /auth/account/summary`), Security & Sessions (MFA status from the authoritative `GET /mfa/status`; password change and session listing shown as honest "not available in this release" — never fabricated controls), Organization context (from `GET /organizations/current`, membership role read-only, link to org settings), and the CERTIFIED Danger Zone (deletion preview/confirm, sole-Owner block and co-Owner eligibility preserved verbatim, exact `DELETE` confirmation, auth cleared + redirect on success). New backend surface is minimal: `AccountProfileService` returning only safe `User` fields (id/email/displayName/createdAt/updatedAt) with identity derived exclusively from `req.user.sub` (forged body `userId` stripped), display-name PATCH validated (1–100 chars, trimmed) with MFA/secret fields never touched; no schema migration, no secrets exposed, no `/me`-style endpoint added beyond the self-scoped summary. | No existing endpoint returned safe profile fields (deletion-preview is deletion-only; JWT carries only sub/orgId/role), so the self-scoped summary is the smallest additive contract. Account ≠ Organization: `displayName`/email belong to the person; org name/role belong to the membership — rendering both on one page without conflating identity boundaries. Deferred (no backend, recorded, never faked): email verification status, avatar, last login, password change, session listing/revocation, MFA enrollment UI on this page. | ✅ Done (`ACC-FOUND-01`) |
| D27 | 2026-08-15 | **ACC-FIX-01: the shared `Button` renders `asChild` children through Radix `Slottable` instead of raw multi-child composition.** The Button body is a multi-expression JSX block (`{loading && <spinner/>}`, `{!loading && leftIcon && <span/>}`, children, `{!loading && rightIcon && <span/>}`), so even a single `Link` child reached `Slot` as an ARRAY (with falsy entries) — `React.Children.count` saw 1, but `React.isValidElement(array)` was false, and `@radix-ui/react-slot@1.3.0` threw `Slot failed to slot onto its children` as soon as the OrganizationSection resolved its org. Fix: when `asChild`, `Button` renders exactly one `Slottable` wrapping the semantic child; loading/leftIcon/rightIcon stay as `Slot` siblings (Radix's documented multi-child composition), so all modes keep working with no behavior change. No dependency change — installed `@radix-ui/react-slot` v1.3.0 already exports `Slottable`. | Radix `Slot` requires exactly one React element child or a `Slottable` target; the falsy-expression children defeated the single-element check and crashed the Account page at runtime while jest CI stayed green (web page specs wholesale-mock `@techfusion/ui`, so the real Slot path was never exercised). Slottable is the Radix-supported composition, preserving icon/loading markup and Next.js Link semantics. | ✅ Done (`ACC-FIX-01`) |
| D28 | 2026-08-16 | **ACC-SEC-02B1 — MFA core security hardening.** **(a) Secrets encrypted at rest with a versioned marker: `enc:v1:` + envelope-encrypted base32 via the existing `EncryptionService` (AES-256-GCM, key `MASTER_KEY || AI_ENCRYPTION_KEY`).** Legacy plaintext base32 rows (and historical `iv:authTag:ciphertext` values) remain readable for backward compatibility and are re-encrypted in the same transaction only after a successful possession-proven verification; decryption failure fails closed (deterministic 500, generic message, no leak; login challenge denied). The setup secret is returned only from the enroll response, never persisted or logged. **(b) Enroll/verify throttled: `@Throttle(mfaThrottle())` = 5/60 s per IP from the previously-unused `STRICT_RATE_LIMITS.mfa`; `mfaThrottle()` deliberately returns real limits in test mode (unlike `throttle()`, which neuters to 999999) so deterministic 429s are provable — only the MFA routes reference it, so no other route's test behavior changes.** **(c) Deterministic lifecycle: 409 enroll/verify while enabled, 400 not-enrolled, pending (never-enabled) re-enrollment allowed, enabled secret never silently replaced.** (d) Structured security events `mfa_enrollment_started`/`mfa_enabled`/`mfa_verification_failed` (reason-tagged), zero credential material in logs/responses. | A DB leak must not expose an active TOTP secret (same verifier-only principle as D16); tests must prove enforcement (test-mode throttling is otherwise disabled); UNKNOWN/deny beats false certainty on an unreadable secret; no schema change, no migration, no recovery-code intro (mfaBackupCodes untouched), no MFA UI — enrollment UI remains deferred (T25). | ✅ Done (`ACC-SEC-02B1`) |
| D29 | 2026-08-16 | **ACC-SEC-02B2 — MFA recovery lifecycle.** **(a) Operation-scoped password re-authentication, server-authoritative:** `ReauthenticationService.verifyPassword(userId, password)` — identity derived exclusively from the verified JWT `req.user.sub` (forged body `userId` ignored), bcrypt compare vs `User.passwordHash`, deterministic 401 `'Current password is incorrect'` for both wrong-password and unknown-user (no account enumeration), `reauthentication_failed` event; no stored "recently re-authenticated" session state. **(b) Hashed one-time recovery codes, no schema change:** `User.mfaBackupCodes` TEXT stores a JSON array of SHA-256 hex hashes only (domain prefix `techfusion:mfa-recovery:v1:`), never plaintext; 10 codes × 16-char base32 (A-Z2-7) grouped `XXXX-XXXX-XXXX-XXXX` (80 bits/code), unbiased generation (256 % 32 = 0), plaintext returned exactly once from generate/regenerate; `GET /mfa/recovery-codes/status` exposes only `{generated, availableCount}`; generate/regenerate/disable throttled 5/60 s via `mfaThrottle()`. **(c) Atomic single-use consumption:** `SELECT … FOR UPDATE` row lock inside the Prisma transaction, matched hash removed before commit — a code can never be used twice, even under concurrent attempts (Promise.all race proven in tests). **(d) Disable requires re-authentication + a second factor:** password AND valid TOTP OR unused recovery code; verification order is enabled-check → password (401) → second factor (400); the recovery path clears `isMfaEnabled`/`mfaSecret`/`mfaBackupCodes` in the same transaction that consumes the code. **(e) Recovery login:** `POST /auth/verify-login` accepts `recoveryCode` and mints access/refresh tokens only after consuming the code inside the same transaction (failed challenge rolls back the consumption). **(f) Fail-closed on undecryptable secrets:** a corrupt `enc:v1:` envelope → deterministic 500, state untouched, no material leaked. Events: `mfa_recovery_codes_generated`/`mfa_recovery_codes_regenerated`/`mfa_recovery_code_used`/`mfa_disabled`/`mfa_disable_failed`/`reauthentication_failed`. | A locked-out user needs a possession-proven recovery path, and sensitive writes must re-prove password possession (T28). Storing only verifiers matches the D16/D28 verifier-only principle; a DB leak cannot yield usable codes. One-time use must hold under concurrency (FOR UPDATE). Re-auth is operation-scoped (no fake "recent auth" window — UNKNOWN beats false certainty, principle 3). Password change/reset, session management, and the MFA account-page UI remain deferred (T25). No schema change, no migration, no frontend. | ✅ Done (`ACC-SEC-02B2`) |
| D30 | 2026-08-16 | **ACC-UX-02C — interactive account security experience on the account page.** **(a) MFA enrollment** (`mfa/MfaEnrollmentDialog.tsx`): intro → setup (QR rendered from the backend `qrCode` data URL as `<img>` — no new dependency; masked setup key with reveal + copy) → TOTP verify (`POST /mfa/verify`) → success chains into recovery-code generation. **(b) One-time recovery codes** (`RecoveryCodesDialog.tsx` + `OneTimeCodes.tsx`): password + TOTP challenge before generate/regenerate; the 10 codes render exactly once from the backend response (never persisted, never re-fetched); per-code + copy-all via `clipboard.ts` `copyText`. **(c) Disable** (`DisableMfaDialog.tsx`): password AND TOTP **or** unused recovery code; exactly one second factor is ever sent (matching the backend DTO). **(d) `SecuritySection.tsx` is a self-contained state machine** with NO optimistic status updates — every mutation refetches `GET /mfa/status` + `GET /mfa/recovery-codes/status`; the regenerate dialog opens only when recovery status reports `generated === true` (a real UI bug found by the new suite: a never-generated status previously offered "Regenerate"). **(e) Recovery-mode login** (`LoginMfaStep.tsx` + `LoginForm.tsx`): toggle between authenticator code and recovery code; the `verify-login` body carries exactly `{ userId, recoveryCode }` (normalized `XXXX-XXXX-XXXX-XXXX`), never `token`; switching mode clears the typed code; "Use a different account" resets mode to TOTP. **(f) Typed error mapping** (`mfa-errors.ts`): backend copy passed through only for 400/401/403/404/409; calm copy for 429/5xx/network/unknown; `AbortError` → cancelled; no raw internals surface. **(g) Section nav** (`AccountSectionNav.tsx` + `account-sections.ts`): URL-fragment anchors with `aria-current`; sections `scroll-mt-6`; DangerZone sole-Owner blocker rows link the real `/dashboard/settings/organization`. New/extended web suites: `account-page.spec.tsx` (27), `security-section.spec.tsx` (34), `mfa-client.spec.ts` (12), `mfa-errors.spec.ts` (11), `login-page.spec.tsx` (35) — built failing-then-passing (the suite exposed the regenerate-dialog bug before it was fixed). | The backend MFA surface (ACC-SEC-02B1/B2) was untestable and unusable from the product UI; D26 kept MFA status read-only pending exactly this UI. Recovery codes are shown exactly once by design (plaintext must not persist client-side — verifier-only principle D16/D28 extended to the browser); no optimistic statuses avoid false certainty (principle 3); the UI sends only server-issued identity, never a body `userId`/`orgId` (principle 8). Password change, session management, email verification, avatar, last-login remain deferred (no backend; T25). No backend/schema changes; no new dependencies; no push/deploy. | ✅ Done (`ACC-UX-02C`) |
| D31 | 2026-08-16 | **ACC-SEC-02D2A — session identity & refresh-token hardening (backend foundation).** **(a) Stable `sessionId`, additive `sid` claim:** `RefreshToken.sessionId` is a server-generated UUID (NON-SECRET, deliberately NOT unique — the whole rotation chain shares one) that survives login → refresh → refresh…; every minted access JWT carries `sid` = that `sessionId`. The `sid` claim is additive and **non-authoritative**: guards still require only `sub`+`orgId`, so pre-stage access tokens without `sid` remain accepted until their natural 15 m expiry (P6 proof). **(b) Refresh-token verifier-only storage:** only `rt:v1:<sha256-hex>` is persisted (D16-style); the raw 96-hex token is returned exactly once over TLS and never stored. Refresh resolves by verifier first, then a single exact raw-token lookup is the controlled **legacy plaintext compatibility path**; the legacy row is atomically upgraded to verifier-only storage inside the same CAS rotation (`updateMany {id, revokedAt:null} → {revokedAt, token:verifier}`), so no raw value remains at rest even under concurrent rotation (P11/P12/P13). **(c) Truthful server-observed session metadata:** `lastUsedAt` (every login/refresh), `ipAddress` (first-seen preserved across rotation), `userAgent` (300-char cap, latest), `deviceName` reserved and never fabricated; metadata is captured only from the request environment (`x-forwarded-for` first entry validated ≤45 chars, else direct peer) — **never from a request body** (body injection of `deviceName`/`ipAddress`/`userAgent`/`sessionId` proven inert, P16). NO trust-all-proxy global flag; Railway/proxy spoofing limitation documented (T31). **(d) Additive migration `20260816210000_refresh_session_identity`:** 5 new columns, NOT NULL `sessionId` backfilled with `gen_random_uuid()` (idempotent), indexes `(userId, revokedAt)` + `(userId, sessionId)`; validated on a scratch pre-change DB seeded with a legacy plaintext row. 20-proof spec `test/refresh-token-hardening.spec.ts`, failing-then-passing (17 failed pre-fix → 20 passed); full API suite 64/1099 green, lint/build clean, V1 gate 18/19 PASS (sole failure pre-existing TOTP time-window flake). **Not built (deliberately):** password change, session list/revoke-one/revoke-others/revoke-current endpoints, frontend, cookie migration, access-token denylist — ACC-SEC-02D2B scope. | Same verifier-only principle as D16/D28/D29 extended to refresh tokens: a DB leak must not expose a usable credential. CAS rotation is preserved; `sessionId` is not a secret and its chain-stability is the unit for future session revocation (D2B). Server-observed metadata avoids client-forged claims (principle 8); no trust-all-proxy keeps the security boundary intact, with the proxy spoofing limit documented (principle 7 — security boundary wins, UX/forensics gap documented). Backward-compatible: additive columns, optional fields, non-authoritative claim (principle 9). | ✅ Done (`ACC-SEC-02D2A`) |

## Open Founder Decisions

1. **Tier naming** — rename `PREMIUM_TIER_PLACEHOLDER` (D3).
2. ~~**SSO strategy** — disable route vs. implement real SAML/OIDC (D5 sibling; Stage 01-SUB-01).~~ **DONE — disable fail-closed (D11/D12).** Remaining: decide when to schedule a real SAML/OIDC verification substage (future; not P0 for this substage).
3. ~~**RLS strategy** — implement vs. remove (D5).~~ **DONE — Option B: app-layer authoritative, RLS kept inert as defense-in-depth (D14, `V1-STAGE-01-SUB-02`).**
4. **Windows scope** — confirm P0 or move to P1 (D6).
5. **Free-tier feature surface** — network/inventory/security on Free? (D8).
6. **Remote support V1 shape** — consent-gated real control vs. explicit "pending" state (Stage 06).

## Appended After This Mission

This log is the canonical place for future substage decisions; each completed stage appends a row.
