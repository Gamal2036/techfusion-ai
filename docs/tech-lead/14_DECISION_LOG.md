# 14 — Decision Log

Chronological record of architectural and governance decisions. First entry covers the V1-MASTER-ROADMAP-00 discovery mission (2026-08-09).

| # | Date | Decision | Rationale | Status |
|---|------|----------|-----------|--------|
| D1 | 2026-08-09 | Documentation-only discovery mission; no product code changed, no refactors, no deletions. | Mission mandate; evidence must precede change. | ✅ Done |
| D2 | 2026-08-09 | `docs/tech-lead/` is the authoritative technical-lead documentation area; legacy root `TF_*.md` reports and `TECHFUSION_V1_READINESS_AUDIT.md` are historical, not authoritative. | Single source of truth; avoid duplicate conflicting docs. | ✅ Done |
| D3 | 2026-08-09 | Highest commercial tier is referenced as `PREMIUM_TIER_PLACEHOLDER`; the internal name "GM1" is not used as a commercial name. | Founder will rename; no hard-coded prices. | ⏳ Open (founder names tier) |
| D4 | 2026-08-09 | V1-STAGE-01 (security/tenancy/credentials) is the next stage; SSO remediation is the first substage. | S1 is an active auth bypass and gates everything. | ⏳ Pending implementation |
| D5 | 2026-08-09 | RLS decision deferred to Stage 01: either implement (transactional `set_config` + non-owner role + FORCE) or remove decorative policies + app-layer isolation suite. | RLS is inert today; both options are valid with different risk. | ⏳ Open (founder/AI lead decision at Stage 01) |
| D6 | 2026-08-09 | Windows agent is P0 because "Linux AND Windows production support" is an explicit launch requirement. | Mission constraint. Founder may re-scope to P1. | ⏳ Open (founder) |
| D7 | 2026-08-09 | Stage ordering differs from the brief: identity/RBAC moved into existing certified base (not a new early stage); deploy/CD reliability and billing moved earlier; Windows placed after fleet-reliability groundwork. | Evidence shows RBAC/membership already certified; CD is undeployable; billing gaps are paid-launch gates. | ✅ Accepted |
| D8 | 2026-08-09 | Free-tier feature surface (whether network/inventory/security are free or PRO) requires founder sign-off before Stage 03 entitlement work. | Current `PLAN_CONFIGS` does not gate these; the mapping is a commercial decision. | ⏳ Open (founder) |
| D9 | 2026-08-09 | No commit of `apps/api-gateway/.env.test`; it remains untracked (test placeholders). | Working-tree hygiene; gitignore exception is intentional. | ✅ Accepted |
| D10 | 2026-08-09 | One documentation-only commit created for this mission. | Isolated clean docs change; per git safety rules. | ✅ Done |
| D11 | 2026-08-09 | **SSO strategy (founder): do NOT implement real SAML/OIDC for Production V1. Disable the unsafe `POST /auth/sso/login` path FAIL-CLOSED.** The incomplete login must never issue a session from client-supplied identity. SSO domain/database architecture (SsoConfig, admin config routes, User.sso*) is preserved for a future real implementation. | S1 is an active P0 auth bypass; real IdP verification is out of scope for this substage. | ✅ Done (`V1-STAGE-01-SUB-01`) |
| D12 | 2026-08-09 | Fail-closed mechanism = keep the `POST /auth/sso/login` route registered and `@Public()`, but have `SsoService.ssoLogin` throw a deterministic `501 Not Implemented` BEFORE touching any data (no org/config/user reads → zero information leak; slug/enabled/existence indistinguishable). Route removal rejected: preserving the route keeps an intact contract for the future SAML/OIDC implementation, and 501 is a better client contract than 404. Insecure code (`generateRefreshToken`, `ensureMembership`, imports) deleted so the exploit cannot be re-triggered by any caller. | Deterministic, safe, architecture-preserving; consistent with NestJS error architecture. | ✅ Done |
| D13 | 2026-08-09 | SSO is classified **DISABLED_SAFE** (not CERTIFIED). Re-enablement is gated on a future substage implementing the verification contract in `V1-STAGE-01-SUB-01_SSO_REMEDIATION_REPORT.md` §7 (OIDC issuer/audience/JWKS/exp/nonce+PKCE; SAML signature/issuer/audience/destination/validity/replay; JIT + linking only after verified identity). | Avoids a false "certified" claim; SSO must not be marketed/used until verified. | ✅ Done |
| D14 | 2026-08-10 | **RLS (Option B): app-layer isolation is AUTHORITATIVE; RLS is kept as non-authoritative defense-in-depth (no migration, no FORCE, no `set_config`).** Empirical proof from `V1-STAGE-01-SUB-02`: the app DB role is `SUPERUSER`+`BYPASSRLS` (32 tables RLS-enabled, 0 FORCE) so policies are never consulted; `SET app.current_org_id` + `FORCE RLS` still returned both orgs' rows to the superuser; only a `NOBYPASSRLS` non-owner role was filtered. Prisma's connection pooling cannot carry session settings safely (set_config is per-connection/transaction-only). Boundaries enforced by membership/device-authoritative `orgId` scoping + `test/cross-tenant-isolation.spec.ts` (20 tests) + worker org re-verification. | Closing D5 (founder/AI lead asked for the decision; empirical RLS proof made Option B the low-risk choice; Option A (set_config + non-owner role + FORCE) remains viable if the Prisma pooling/role model ever changes, re-run isolation suite against a non-owner role first). | ✅ Done |
| D15 | 2026-08-10 | Fix confirmed cross-tenant defects with **atomic scoped writes** (`updateMany`/`deleteMany` on `{id, orgId}`) instead of find-then-mutate, and make the **AI router state per-org** (strategy/stats keyed by `orgId`). Worker processors now **re-verify payload resource IDs against `orgId`** before acting (backup verify/restore/execute, KB embedding; security `deviceId` bound to the verified row). | Removes TOCTOU races and cross-tenant state leakage; follows the existing `processSecurityJob` safe pattern; no endpoint route/semantics changed for already-correct paths. | ✅ Done |
| D16 | 2026-08-10 | **Device credentials are verifier-only: drop the plaintext `Device.deviceToken` column and authenticate by SHA-256 `deviceTokenHash` exclusively, fail-closed.** Guard and `findByToken` have no plaintext equality fallback; register/register-public/rotate persist only the hash; the one-time raw token is returned to the device over TLS at issuance and never stored. Rows without a stored verifier fail closed (401) and recover via `POST /devices/recover-credential` / duplicate re-registration / agent re-enrollment. No plaintext→hash backfill was performed — the 20260720130000 migration already hashed all rows, and a one-way hash cannot be derived from an unknown token; `CredentialRotationEvent.oldTokenHash` uses sentinel `legacy-no-verifier` when rotating a verifier-less row. | Closes S3 (DB leak exposes live device credentials): with no plaintext column, a DB leak cannot expose a usable credential; equality fallback removed so no device can authenticate without a stored verifier; rotation/recovery keeps enrollment and re-enrollment working (kept `deviceTokenHash` nullable-@unique, no API contract change for agents). | ✅ Done (`V1-STAGE-01-SUB-03`) |
| D17 | 2026-08-10 | **Device telemetry ingestion is server-authoritative: `POST /devices/metrics` binds identity/org exclusively to the verified `Device` row (`req.device.id`/`req.device.orgId`) and treats the client payload as data only.** Malformed telemetry must fail deterministically 4xx, never 500: fractional byte/counter values and unvalidated `fans.rpm` (which previously hit `BigInt` RangeError / Prisma validation → 500) are now `@IsInt()`/nested-DTO validated; the inert body `deviceToken` field is removed (guard authenticates by header); DEV-only auth logs print token/hash prefixes no longer (presence/length/counts only). No migration required — validation-only + guard-log change. | Closes the remaining Stage-01 trust-boundary gap: with the DTO carrying no `deviceId`/`orgId` and whitelist stripping unknown fields, no client-supplied value can impersonate a device/tenant; deterministic 4xx prevents 5xx error storms from a buggy/malicious agent; raw credentials stay out of logs/errors/responses. SUB-02 isolation + SUB-03 hash-only credential guarantees preserved. | ✅ Done (`V1-STAGE-01-SUB-04`) |
| D18 | 2026-08-10 | **Metrics auth is header-only and fail-closed (S5): `GET /metrics` authenticates via `Authorization: Bearer` only; the query-string `?token=` input is removed; when `METRICS_AUTH_TOKEN` is set a non-matching/absent Bearer header → 403; in `NODE_ENV=production` an unset `METRICS_AUTH_TOKEN` → 403 (endpoint not publicly exposed until explicitly configured).** Dev (non-production, token unset) stays open for local/`prometheus.yml` scraping. Worker metrics (port 9464, cluster-internal, no ingress) keeps the query-token pattern for now — deferred, documented residual. Env is read per-request (no stale module-load snapshot). | A token sent in the query string is captured by proxies and by request-URL logging; the Helm ingress maps `api.techfusion.ai/` → api-gateway Prefix `/`, so `/metrics` is publicly reachable in the current topology. Header-only auth keeps the secret out of URLs/logs; fail-closed production requires explicit `METRICS_AUTH_TOKEN` provisioning (Stage-02 wiring adds it to Helm / ServiceMonitor). | ✅ Done (`V1-STAGE-01-SUB-05`) |
| D19 | 2026-08-10 | **Dev-only `scripts/seed-inventory-test.ts` JWT fallback is a non-production placeholder** (`tfenr_ci-secret-replace-before-deploy`, exempt from the secret scanner), not a real credential. | The previous 64-hex fallback looked like a real signing key; the placeholder is unambiguous and scanner-exempt; the script is dev-only and never deployed. | ✅ Done |
| D20 | 2026-08-10 | **Committed `backups/` artifacts and the tracked-only secret scanner are kept as documented residuals (P3/LOW).** The 60 backup artifacts were audited — no real credentials (Device tables empty, test bcrypt hashes only) — so history rewrite is not justified and retention is a founder decision (T12). The scanner stays `git ls-files`-only (T20): CI has no untracked files and `.env.test` is intentionally untracked (D9); extending it adds local false-positive risk with no CI value. Deploy-secret wiring (T1–T3) is deferred to `V1-STAGE-02`. | Fail-closed guarantees are already enforced at startup (`validateEnvironment`), in Helm (`required`), and in CI; the remaining items are deploy-wiring debt that belongs to Stage-02, not a secrets exposure. **V1-STAGE-01 is CLOSED.** | ✅ Done (`V1-STAGE-01-SUB-05`) |
| D21 | 2026-08-11 | **Stage-02 priority decision (founder): Enrollment / Token / Device-Link / Presence reliability is promoted AHEAD of Deployment/CD within V1-STAGE-02.** Trustworthy device identity is a prerequisite for Cybersecurity, Network, Monitoring and other device-backed modules. The Deployment/CD work is preserved as a LATER Stage-02 substage (`V1-STAGE-02-SUB-02`), not deleted. | Device identity/presence reliability is a capability foundation; shipping device-backed modules on an untruthful presence model would compound defects across Cybersecurity/Network/Monitoring. CD repairs remain a Stage-02 requirement, just serialized after the identity foundation. | ✅ Done (`V1-STAGE-02-SUB-01`) |
| D22 | 2026-08-11 | **Presence truthfulness: `Device.lastSeenAt` is nullable with no default and is set ONLY by a verified authenticated telemetry heartbeat.** Registration no longer writes `lastSeenAt` (previously `@default(now())`), so a Device row never implies ONLINE. A registered-but-never-heartbeat device derives **UNKNOWN**; null-safe ordering is nulls-last everywhere; worker presence-state and web presence surfaces already map null → UNKNOWN. Migration `20260810120000_device_lastseen_nullable_presence_truth` (DROP NOT NULL + DROP DEFAULT); existing timestamps preserved. Hostname is removed from device identity matching (`findExistingDevice`); credential recovery requires strong identity (identityFingerprint/installationId), hostname/deviceId alone rejected. Duplicate/concurrent registration collapses to one row (P2002 → idempotent reuse) without weakening Stage-01 hash-only credential model. | `lastSeenAt @default(now())` fabricated presence for every enrolled device — a registration with no agent heartbeat looked ONLINE. UNKNOWN is preferable to false certainty (AGENTS.md principle 3); hostname/deviceId alone cannot authorize a credential handover (principle 8); race-safe registration prevents duplicate device identities. | ✅ Done (`V1-STAGE-02-SUB-01`) |
| D23 | 2026-08-11 | **Cybersecurity real-device flow is deterministic and truthful (CYBER-01). (a) Web terminal-state machine in `useSecurity.ts`: `idle`/`triggering`/`running`/`completed`/`failed`/`timeout`.** `completed` and `failed` stop polling only when a terminal scan is NEW since the trigger (baseline-scan-id guard so a pre-existing completed scan does not prematurely end a re-run); a 120 s client backstop converts a stuck pending scan into an honest `timeout`/retry state; 401/403 and unexpected API failures stop polling and surface an honest error banner (never infinite loading); polling resets on device change; empty successful scans render truthfully ("No findings — clean posture"). **(b) `GET /security/latest/:deviceId` contract extended additively: it now returns the latest TERMINAL scan (`status IN (completed, failed)`, ordered by `completedAt` desc) and exposes the scan `error` field; pending scans still return 404 while running, and the completed path is unchanged (backward compatible).** (c) Agent: source already sent `Authorization: Bearer <persistent-device-credential>` on `GET /security/pending/:deviceId` (`client.rs:484`); change was TEST-ONLY — the deployed binary is stale and must be rebuilt for the manual retest. | The Web polling loop had no terminal model — failed/stuck pending scans left the UI permanently loading, and 401/403 never stopped polling; `getLatestScan` filtered completed-only so a failed terminal scan was invisible to polling. UNKNOWN is preferable to false certainty, and the security boundary (DeviceTokenGuard, device/org authority) is preserved — no token/auth architecture change, no fabricated findings. | ✅ Done (`V1-STAGE-02-SUB-01A` / CYBER-01) |

## Open Founder Decisions

1. **Tier naming** — rename `PREMIUM_TIER_PLACEHOLDER` (D3).
2. ~~**SSO strategy** — disable route vs. implement real SAML/OIDC (D5 sibling; Stage 01-SUB-01).~~ **DONE — disable fail-closed (D11/D12).** Remaining: decide when to schedule a real SAML/OIDC verification substage (future; not P0 for this substage).
3. ~~**RLS strategy** — implement vs. remove (D5).~~ **DONE — Option B: app-layer authoritative, RLS kept inert as defense-in-depth (D14, `V1-STAGE-01-SUB-02`).**
4. **Windows scope** — confirm P0 or move to P1 (D6).
5. **Free-tier feature surface** — network/inventory/security on Free? (D8).
6. **Remote support V1 shape** — consent-gated real control vs. explicit "pending" state (Stage 06).

## Appended After This Mission

This log is the canonical place for future substage decisions; each completed stage appends a row.
