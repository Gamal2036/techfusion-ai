# 10 — Technical Debt Register

Status: 2026-08-17. **Nothing in this register was deleted or fixed during the discovery mission.** Items are reported for controlled remediation only. Subsequent security substages fixed T8 (`SUB-01`), T9/T14 (`SUB-02`), T10 (`SUB-03`); `SUB-05` (2026-08-10) fixed T11 and audited T12/T20 (see annotations); `ACC-SEC-02B1` (2026-08-16) added and fixed T27; `ACC-SEC-02B2` (2026-08-16) annotated T25 (recovery/disable backend now exists) and added T28; `ACC-UX-02C` (2026-08-16) annotated T25 (account-page MFA UI now exists); `ACC-SEC-02D2A` (2026-08-16) annotated T25/T28 (session identity + refresh-token hardening backend now exists) and added T29/T30; `FIX-AUTH-LOGOUT-01` (2026-08-17) fixed T32.

| # | Area | Item | Severity | Evidence |
|---|------|------|----------|----------|
| T1 | CD/deploy | **Helm chart not deployable as written.** `templates/secrets.yaml` uses `required` on postgresql.password/jwtSecret/jwtRefreshSecret/encryptionKey — none supplied by workflows or values. Deployment image refs `ghcr.io/techfusion-ai/<svc>` ≠ pushed `ghcr.io/Gamal2036/techfusion-ai/<svc>`; `--set image.tag` sets an unused top-level value. | HIGH | `infra/k8s/templates/secrets.yaml:10-13`, `values.yaml`, `cd-*.yml` |
| T2 | CD/deploy | Prod migration initContainer uses `prisma db push --accept-data-loss` (data-loss flag in prod; CI contract is `migrate deploy`). | HIGH | `infra/k8s/templates/api-gateway/deployment.yaml:39` |
| T3 | CD/deploy | Agent deployment DATABASE_URL password = username; missing secret ref. | HIGH | `infra/k8s/templates/agent/deployment.yaml:40` |
| T4 | CD/deploy | CD health checks use `kubectl run -it` (TTY not available on runners). | MEDIUM | `cd-staging.yml:69-71`, `cd-production.yml:69-71` |
| T5 | Queues | REPORT queue: producer missing + worker delegates to non-existent `POST /reports`. | HIGH | `06`; `queue.service.ts:8,86`; `processors.ts:144` |
| T6 | Queues | KB_EMBEDDING: `POST /ai/embed` missing; silent deterministic mock vectors written to DB. | HIGH | `06`; `processors.ts:1295-1320` |
| T7 | Queues | Queue names/thresholds duplicated across gateway/worker/web (`queue.constants.ts`, `queue-names.ts`, presence files) — drift risk; `REINDEX` constant already drifted. | MEDIUM | `06` |
| T8 | Security | SSO login bypass (S1). | CRITICAL | `07` |
| T9 | Security | RLS inert (S2); decorative policies. | MEDIUM | `07` |
| T10 | Security | Plaintext `Device.deviceToken` + fallback lookup (S3). | MEDIUM | `07` |
| T11 | Security | ~~Metrics token in query string; optional auth (S5).~~ **FIXED `V1-STAGE-01-SUB-05`: metrics auth is header-only (`Authorization: Bearer`) and fail-closed (403 in production when `METRICS_AUTH_TOKEN` unset); 8-test suite.** | `07`; `test/metrics-auth-security.spec.ts` |
| T12 | Data | `backups/` — 60 committed DB/config backup artifacts in VCS (incl. old pg/redis dumps). **`SUB-05` audited: no real credentials in any artifact (Device tables empty; test bcrypt hashes only); kept intentionally — history rewrite not justified; retention is a founder decision (P3 residual).** | root `backups/`; `V1-STAGE-01-SUB-05` §1/§7 |
| T13 | Data | Legacy root reports contradict current state (`TECHFUSION_V1_READINESS_AUDIT.md` predates CI; `infra/k8s/README.md` "Phase 14" claim stale; `launch-checklist.md` references non-existent release `techfusion-api-gateway`). | LOW | `02` |
| T14 | Code | `demo.controller.ts` dead RBAC demo (SCAFFOLD); `RolesGuard`/`@Roles` dead code (zero usages). | LOW | grep + read |
| T15 | Code | Legacy `Dockerfile.web` unused (CI/compose use `apps/web/Dockerfile`). | LOW | `02` |
| T16 | Data model | `Organization.plan` + `Subscription` dual source of truth (mitigated by STAGE-01A integrity work). | MEDIUM | `schema.prisma` |
| T17 | Agent | Agent has no self-update; version only sent at registration (server unaware of upgrades). | MEDIUM | `05` |
| T18 | Agent | Temperature/battery/load/service fields hardcoded `None`. | LOW | `collector.rs` |
| T19 | Frontend/backend contract | `CpuMetricsDto` whitelist strips agent CPU model; duplicated presence constants risk drift (tests mitigate). | LOW | `metrics-payload.dto.ts`, `device-presence.ts` |
| T20 | Tests | Secret scan only covers `git ls-files` — untracked-but-unignored `.env.test` never scanned (contains test placeholders only today). **`SUB-05` assessed and kept (accepted residual): CI checks out a clean tree (no untracked files), and `.env.test` is intentionally untracked (D9); extending the scanner to untracked files would add local false-positive risk with no CI value.** | `ci-secret-scan.sh:32`; `V1-STAGE-01-SUB-05` §7 |
| T21 | Tests | No end-to-end tests for SSO login; `actionlint` claim unverifiable (only YAML parse). | MEDIUM | `07`, `02` |
| T22 | Misc | Malformed leftover file at repo root: `tablish TechFusion V1 enterprise foundation and command center"` (untracked). | LOW | root listing |
| T23 | Billing | `maxTeamMembers` / `maxAlertRules` unenforced. | MEDIUM | `09` |
| T24 | Misc | `engines.node ">=18"` vs Node 22 pin; `forceExit` in test scripts (flakiness masking). | LOW | `package.json`, app manifests |
| T25 | Account profile | **Deferred account capabilities — no backend support, surfaced honestly on `/dashboard/settings/account` as "not available in this release" (no fabricated controls).** Email verification status, avatar/profile photo, last-login, password change endpoint, session listing/revocation, and MFA enrollment/management UI on the account page are all absent (`User` has no email-verified/avatar/last-login fields; no password-change or session endpoints exist; MFA status is read-only via `GET /mfa/status`). Recorded as deferred per ACC-FOUND-01, never faked. **Annotation `ACC-SEC-02B2` (2026-08-16): the backend recovery/disable surface now exists** — `POST /mfa/disable` (password + TOTP-or-recovery-code), `POST /mfa/recovery-codes/generate|regenerate`, `GET /mfa/recovery-codes/status`, recovery login via `POST /auth/verify-login` `recoveryCode`, and operation-scoped re-authentication. **Annotation `ACC-UX-02C` (2026-08-16): the account-page MFA UI now exists and is interactive** — enrollment, disable, recovery-code display/regeneration, and recovery-mode login are implemented and tested (`security-section.spec.tsx` 34, `account-page.spec.tsx` 27, `login-page.spec.tsx` 35). **Annotation `ACC-SEC-02D2A` (2026-08-16): the session-identity backend now exists** — stable `sessionId` (+ additive non-authoritative `sid` JWT claim) and truthful server-observed session metadata (`lastUsedAt`/`ipAddress`/`userAgent` on `RefreshToken`; `deviceName` reserved). **Annotation `ACC-SEC-02D2B` (2026-08-17): password change and session management backend now exists** — `POST /auth/change-password` (reauth + revocation + reissue), `GET /auth/sessions`, `DELETE /auth/sessions/:sessionId`, `DELETE /auth/sessions` (others), `DELETE /auth/sessions/current`; 30-proof spec. **Annotation `ACC-UX-02D3` (2026-08-17): password change and session management frontend now exists** — `PasswordChangeDialog`, `ActiveSessions`, `security-client.ts`, `useAccountSecurity.ts`; `SecuritySection` integrated; 43-proof spec. **Remaining deferred:** email verification, avatar, last-login UI (no backend). | LOW | `prisma/schema.prisma` `User` (84-105), `src/auth`, `src/mfa`, `src/reauthentication`, `src/account`; `03` |
| T26 | Tests | **Web page specs wholesale-mock `@techfusion/ui` (`account-page.spec.tsx` and 11 other specs), which hid the real `Button`/Radix-`Slot` runtime crash (ACC-FIX-01) from jest CI.** The mock `Button` renders `<button>{children}</button>` and ignores `asChild`, so the shared-UI `Slot` path was never exercised by the Account page suite. Fixed by adding regression specs that exercise the REAL shared Button (`packages/ui/src/__tests__/Button.test.tsx` 7 tests, `apps/web/src/__tests__/organization-section.spec.tsx` 3 tests, both failing pre-fix); the broad page-level mocks remain and can keep hiding future shared-UI runtime defects. | LOW | `jest.mock('@techfusion/ui', …)` in 12 web specs incl. `account-page.spec.tsx`; `packages/ui/src/components/Button.tsx` |
| T27 | Security | **MFA secrets were stored as plaintext base32 — FIXED `ACC-SEC-02B1` (2026-08-16).** Enroll/verify throttling also absent — the `STRICT_RATE_LIMITS.mfa` constant was declared but never wired (an unused implied-protection constant). Fix: secrets encrypted at rest (`enc:v1:` envelope via `EncryptionService`, key `MASTER_KEY || AI_ENCRYPTION_KEY`); legacy plaintext rows remain readable and are upgraded only after a successful possession-proven verification; decryption fails closed (deterministic 500, no leak); `@Throttle(mfaThrottle())` 5/60 s on enroll + verify; deterministic lifecycle (409 enroll/verify while enabled, 400 not-enrolled, pending re-enrollment allowed); structured events `mfa_enrollment_started`/`mfa_enabled`/`mfa_verification_failed`. 24-test suite `test/mfa-security.spec.ts`. | MEDIUM | `src/mfa/*`, `src/auth/auth.service.ts`, `src/config/rate-limits.ts`; `14` D28 |
| T28 | Security | **No MFA recovery/disable lifecycle existed — FIXED `ACC-SEC-02B2` (2026-08-16).** A user locked out of TOTP had no path to recover access or disable MFA (and no sensitive write required re-verification of possession). Fix: operation-scoped re-authentication (`src/reauthentication/reauthentication.service.ts` — bcrypt vs `User.passwordHash`, identity from verified JWT only, forged body `userId` ignored, deterministic 401 `'Current password is incorrect'`, no account enumeration, event `reauthentication_failed`); hashed one-time recovery codes (10 × 16-char base32 grouped `XXXX-XXXX-XXXX-XXXX`, only SHA-256 persisted in `User.mfaBackupCodes` TEXT JSON array, prefix `techfusion:mfa-recovery:v1:`, plaintext returned exactly once, single-use via atomic `SELECT … FOR UPDATE` consumption, `GET /mfa/recovery-codes/status` = metadata only); `POST /mfa/disable` (throttled 5/60 s) requires password + valid TOTP or unused recovery code (recovery path clears `isMfaEnabled`/`mfaSecret`/`mfaBackupCodes` atomically); `POST /auth/verify-login` accepts `recoveryCode` (tokens minted only after in-transaction consumption; rollback on failed challenge). No schema change. 22-test suite `test/mfa-recovery.spec.ts`. **Annotation `ACC-SEC-02D2A` (2026-08-16): session identity layered on top** — refresh rotation now flows through the session-chain path (`sessionId` preserved across hops) with server-observed metadata; recovery-code login inherits the same session/metadata path. | MEDIUM | `src/mfa/*`, `src/reauthentication/*`, `src/auth/*`; `14` D29 |
| T29 | Security | **Refresh tokens were stored as plaintext — FIXED `ACC-SEC-02D2A` (2026-08-16).** The database held the exact refresh-token value (96-char hex) at rest (`RefreshToken.token`), so a DB read disclosed usable credentials. Fix: only a SHA-256 **verifier** (`rt:v1:<sha256-hex>`, D16-style) is persisted; the raw token is returned exactly once and never stored. Refresh resolves by verifier first, then a single exact raw-token lookup is the controlled **legacy plaintext compatibility path** that atomically upgrades legacy rows to verifier-only storage inside the same CAS rotation (no raw value remains at rest even under concurrent rotation). 20-proof suite `test/refresh-token-hardening.spec.ts` (failing-then-passing, 17 failed pre-fix → 20 passed). | HIGH | `src/auth/auth.service.ts`, `src/auth/refresh-token.util.ts`, `schema.prisma` `RefreshToken`; `14` D31 |
| T30 | Security | **No session identity existed — FIXED `ACC-SEC-02D2A` (2026-08-16) (backend foundation). FIXED `ACC-SEC-02D2B` (2026-08-17) (session management endpoints).** Refresh rows had no stable session identity, so sessions could not be listed/revoked as a unit later. Fix (additive): `RefreshToken.sessionId` (server-generated UUID, NON-SECRET, deliberately not unique — the whole rotation chain shares one) survives the full refresh chain, carried in access JWTs as an additive, non-authoritative `sid` claim (guards still require only `sub`+`orgId`; pre-stage tokens without `sid` stay valid until natural expiry). Backend metadata foundation: `lastUsedAt`/`ipAddress`/`userAgent` on every login/refresh (server-observed only — `x-forwarded-for` first entry validated else direct peer; NO trust-all-proxy flag, Railway/proxy limitation documented), `deviceName` reserved, never fabricated. **ACC-SEC-02D2B (2026-08-17):** session list/revoke-one/others/current endpoints implemented and tested (30 proofs). Frontend session UI deferred (ACC-UX-02D3). Migration `20260816210000_refresh_session_identity` (backfill + indexes, scratch-DB validated). | HIGH | `schema.prisma` `RefreshToken`, `src/auth/auth.service.ts`, `src/auth/auth.controller.ts`, `test/refresh-token-hardening.spec.ts`, `test/password-session-management.spec.ts`; `14` D31, D33 |
| T31 | Security | **Server-observed `ipAddress` trusts `x-forwarded-for` (single trusted-proxy hop documented, no global trust-all-proxy flag) — DOCUMENTED RESIDUAL `ACC-SEC-02D2A` (2026-08-16).** On Railway/the proxy topology the first `X-Forwarded-For` entry is the platform's value; absent a trusted-proxy allow-list, a client that can reach the gateway directly could spoof the forwarded IP into session metadata. Deliberate per principle 7 (security boundary wins): no global trust-all-proxy mode was introduced. The captured IP is session metadata only — it is not used for authorization/rate-limit binding, so spoofing cannot bypass any security control. Remediation options: trusted-proxy allow-list (`TRUST_PROXY_IPS`/`TRUST_PROXY_HEADER`) + `TRUST_PROXY_HOP_COUNT`, or move to `socket.remoteAddress`-only behind a WAF that rewrites `X-Forwarded-For`. | LOW | `src/auth/auth.controller.ts` (`observedMetadata`), `src/config/`; `14` D31 |
| T32 | Frontend | **Logout race condition: `refreshSession()`, `apiFetch()`, `useSessionGuard`, `useDashboardSummary`, and `useCommandCenterData` fired authenticated requests after `logout()` began, causing "Failed to fetch dashboard summary" console errors and stale UI state. — FIXED `FIX-AUTH-LOGOUT-01` (2026-08-17).** Fix: module-level `_isLoggingOut` flag in `auth-client.ts`, set `true` at the start of `logout()` and cleared on completion; `isLoggingOut()` export checked by all five consumers before initiating authenticated requests. Favicon 404 also fixed (minimal valid `favicon.ico` created). 19/19 regression tests (`logout-request-lifecycle.spec.ts`), full web suite 43/915 green, lint/build clean. **Refined `FIX-AUTH-LOGOUT-01A` (2026-08-17):** the initial `apiFetch` guard returned `new Response(null, { status: 0 })` — invalid per the Fetch spec (Response constructor throws RangeError for status < 200; jsdom doesn't enforce this so tests passed but real browsers would throw). Replaced with a typed `LogoutCancellationError` (exported class extending Error) thrown by `apiFetch`; consumers (`useDashboardSummary`, `useCommandCenterData`) explicitly catch `LogoutCancellationError` alongside `isLoggingOut()` checks as defense-in-depth. 27/27 logout tests, full web suite 43/923, lint/build/V1-gate 19/19 all pass. | LOW | `apps/web/src/lib/auth-client.ts`, `apps/web/src/hooks/useSessionGuard.ts`, `apps/web/src/hooks/useDashboardSummary.ts`, `apps/web/src/hooks/useCommandCenterData.ts`, `apps/web/src/__tests__/logout-request-lifecycle.spec.ts`; `14` D32 |

## 2. High-Risk TODO/FIXME Scan

No TODO/FIXME hotspots with destructive implications were found in critical paths (`INFERRED_FROM_CODE`). The three highest-risk items are all configuration/deploy-time (T1-T3) rather than logic bugs.
