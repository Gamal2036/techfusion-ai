export { theme } from './theme';
export type { TechFusionTheme } from './theme';

export const config = {
  appName: 'TechFusion AI',
  version: '0.1.0',
  environment: process.env.NODE_ENV || 'development',
};
